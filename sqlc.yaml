version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "sql/postgresql/users.sql"
    schema: "sql/postgresql/schema.sql"
    gen:
      go:
        package: "pg"
        out: "pkg/store/pg"
        emit_interface: true